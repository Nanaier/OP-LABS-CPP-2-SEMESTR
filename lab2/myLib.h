#pragma once
#include <iostream>
#include <fstream>
#include <string>
#include <iomanip>
#include <vector>;
using namespace std;

string nameOfFile();
vector <string> split(string, char);
void create(string );
void output(string );
void new_list(string );
void newfile(string );
